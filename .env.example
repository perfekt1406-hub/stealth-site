# Game Proxy Configuration
# Set this to your Raspberry Pi reverse proxy domain to route game URLs through the proxy
# Leave unset to use direct game URLs
PUBLIC_GAME_PROXY_URL=https://games-proxy.local
